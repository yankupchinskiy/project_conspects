1. **Переменным**: создает константу
```c++

   const int MAX_SIZE = 100;
```
2. **Указателям**:
``` c++
const int* ptr1;        // Указатель на константу
int* const ptr2;        // Константный указатель
const int* const ptr3;  // Константный указатель на константу
```

2. **Ссылкам**: ссылка на константу

``` c++
const int& ref = x;
```
3. **Методам**: метод не изменяет состояние объекта


```c++
   int getValue() const;
```

4. **Параметрам функций**: гарантирует неизменяемость
5. **Возвращаемым значениям**