- **Поле класса (атрибут, член-данные)** — это **переменная**, объявленная внутри класса. Она хранит состояние объекта.
- **Доступ к полю:** Аналогичен доступу к методу, но без скобок `()`.
    - `объект.поле;`
    - `указатель_на_объект->поле;`
    - Доступ зависит от **модификатора доступа**.