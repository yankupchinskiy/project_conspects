# Изменение модификатора доступа при наследовании

## Краткий ответ
**Да, можно**, но только в сторону **ужесточения** доступа (public → protected → private). Ослабить доступ (private → protected → public) **нельзя**. Это возможно только для **унаследованных** членов базового класса.

## Как можно изменить доступ

### 1. **С помощью using-декларации**
```cpp
class Base {
public:
    int publicField;
    void publicMethod() {}
    
protected:
    int protectedField;
    
private:
    int privateField;  // Не виден в наследниках
};

class Derived : public Base {
private:
    using Base::publicField;     // public → private
    using Base::publicMethod;    // public → private
    
protected:
    // using Base::protectedField;  // Можно оставить protected
    // using Base::publicField;    // Можно сделать protected
};

int main() {
    Derived d;
    // d.publicField = 10;     // ❌ Ошибка: теперь private
    // d.publicMethod();       // ❌ Ошибка: теперь private
}
```

### 2. **Через тип наследования**
```cpp
class Base {
public:
    void pub() {}
protected:
    void prot() {}
private:
    void priv() {}
};

// Public наследование (по умолчанию)
class PublicDerived : public Base {
    // pub() остается public
    // prot() остается protected
};

// Protected наследование
class ProtectedDerived : protected Base {
    // pub() становится protected
    // prot() остается protected
};

// Private наследование
class PrivateDerived : private Base {
    // pub() становится private
    // prot() становится private
};
```

## Что можно и нельзя

### ✅ **Можно:**
- Public → private
- Public → protected  
- Protected → private
- Public → private → (больше нельзя менять)

### ❌ **Нельзя:**
- Private → protected (private члены не наследуются)
- Private → public
- Protected → public (technically можно через using, но нарушает инкапсуляцию)

## Примеры

### Пример 1: **Сокрытие публичного интерфейса**
```cpp
class Database {
public:
    void connect() { /* ... */ }
    void disconnect() { /* ... */ }
    void rawQuery(const std::string& sql) { /* опасно! */ }
};

class SafeDatabase : private Database {  // Скрываем весь интерфейс
private:
    using Database::rawQuery;  // Делаем приватным
    
public:
    using Database::connect;    // Оставляем публичным
    using Database::disconnect; // Оставляем публичным
    
    void safeQuery(const std::string& sql) {
        validateSQL(sql);
        rawQuery(sql);  // Вызываем приватный метод
    }
};
```

### Пример 2: **Ограничение доступа к методам**
```cpp
class Collection {
public:
    void add(int item) { /* ... */ }
    void remove(int item) { /* ... */ }
    void clear() { /* ... */ }
    void sort() { /* ... */ }
};

class ReadOnlyCollection : private Collection {
public:
    // Только read-only методы
    using Collection::sort;  // Можно сортировать
    
private:
    using Collection::add;    // Скрываем модификацию
    using Collection::remove; // Скрываем модификацию
    using Collection::clear;  // Скрываем модификацию
};
```

## Важные ограничения

### 1. **Нельзя изменить доступ к приватным членам**
```cpp
class Base {
private:
    int secret;  // Не виден наследникам
};

class Derived : public Base {
public:
    // using Base::secret;  // ❌ Ошибка компиляции
};
```

### 2. **Using-декларация действует на все перегрузки**
```cpp
class Base {
public:
    void process(int) {}
    void process(double) {}
    void process(const std::string&) {}
};

class Derived : private Base {
private:
    using Base::process;  // Скрывает ВСЕ перегрузки process
};
```

### 3. **Можно сделать исключения для отдельных перегрузок**
```cpp
class Base {
public:
    void log(int level, const std::string& msg) {}
    void log(const std::string& msg) {}
};

class SecureLogger : private Base {
public:
    // Разрешаем только безопасную перегрузку
    void log(const std::string& msg) {
        Base::log(1, msg);  // Всегда с уровнем 1
    }
    
private:
    using Base::log;  // Скрываем другую перегрузку
};
```

## Практическое применение

### ✅ **Хорошие случаи:**
```cpp
// 1. Реализация паттерна "Невиртуальный интерфейс" (NVI)
class Shape {
public:
    void draw() const {  // Невиртуальный публичный метод
        beforeDraw();
        doDraw();  // Защищенный виртуальный метод
        afterDraw();
    }
    
protected:
    virtual void doDraw() const = 0;  // Для переопределения
    
private:
    void beforeDraw() const { /* ... */ }
    void afterDraw() const { /* ... */ }
};

// 2. Ограничение доступа к unsafe методам
class SafeArray : private std::vector<int> {
public:
    using std::vector<int>::size;
    using std::vector<int>::empty;
    
    int safeAt(size_t index) const {
        if (index >= size()) throw std::out_of_range("...");
        return (*this)[index];
    }
};
```

### ❌ **Плохие случаи:**
```cpp
// Нарушение инкапсуляции базового класса
class Base {
protected:
    int internalCounter;  // Для использования наследниками
};

class Derived : public Base {
public:
    using Base::internalCounter;  // ❌ Выставляем наружу
    // Теперь любой может менять internalCounter!
};
```

## Альтернативные подходы

### 1. **Композиция вместо наследования**
```cpp
// Вместо изменения доступа при наследовании
class SafeDatabase {
private:
    Database db;  // Композиция
    
public:
    void connect() { db.connect(); }
    void disconnect() { db.disconnect(); }
    void safeQuery(const std::string& sql) {
        validateSQL(sql);
        db.rawQuery(sql);
    }
    // rawQuery полностью скрыт
};
```

### 2. **Интерфейсы и патерн Adapter**
```cpp
class IDatabase {
public:
    virtual void connect() = 0;
    virtual void disconnect() = 0;
    virtual ~IDatabase() = default;
};

class DatabaseAdapter : public IDatabase {
private:
    Database db;
    
public:
    void connect() override { db.connect(); }
    void disconnect() override { db.disconnect(); }
    // rawQuery не предоставляем
};
```

## Заключение

**Можно изменить, но осторожно:**
- ✅ Можно **ужесточить** доступ к унаследованным членам
- ❌ Нельзя **ослабить** доступ (кроме спорного protected → public)
- 🔒 Private члены базового класса **невидимы** для наследников
- ⚠️ Изменение доступа нарушает принцип подстановки Лисков (LSP)

**Рекомендация:** Используйте изменение доступа только когда действительно нужно ограничить интерфейс, и предпочитайте композицию наследованию, если требуется кардинально изменить доступ к методам.